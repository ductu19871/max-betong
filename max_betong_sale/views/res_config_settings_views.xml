<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="res_config_settings_view_form_inherit_max_betong_sale" model="ir.ui.view">
		<field name="name">res.config.settings.view.form.max.betong.sale.inherit</field>
		<field name="model">res.config.settings</field>
		<field name="priority" eval="40"/>
		<field name="inherit_id" ref="base.res_config_settings_view_form" />
		<field name="arch" type="xml">
			<xpath expr="//form" position="inside">
                <app data-string="Concrete Load Setting" string="Concrete Load Setting" name="max_betong_sale">
                    <block title="Concrete Load" id="base_extension">
						<setting string="Concrete Load Volume" help="The default weight value is used to divide the load for concrete orders. The value must be greater than 0.">
							<field name="concrete_load_volume"/>
						</setting>
					</block>
                </app>
            </xpath>
		</field>
	</record>
	
	<record id="action_concrete_load_config_settings" model="ir.actions.act_window">
        <field name="name">Settings</field>
        <field name="res_model">res.config.settings</field>
        <field name="view_id" ref="res_config_settings_view_form_inherit_max_betong_sale"/>
        <field name="view_mode">form</field>
        <field name="target">inline</field>
        <field name="context">{'module' : 'max_betong_sale', 'bin_size': False}</field>
    </record>

</odoo>