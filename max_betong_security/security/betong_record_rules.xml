<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <!-- =====================================================================
             RECORD RULES - NHÓM TRẠM
             Mô tả: Nhóm Trạm chỉ thấy và được chuyển trạng thái
             những xe (fleet.vehicle) thuộc trạm mình phụ trách.
             Điều kiện: station_id.user_id = uid (Responsible User của Workcenter).
             ===================================================================== -->

        <!-- <record id="rule_fleet_vehicle_tram" model="ir.rule">
            <field name="name">Fleet Vehicle: Chỉ xem xe của Trạm mình</field>
            <field name="model_id" ref="fleet.model_fleet_vehicle"/>
            <field name="domain_force">['|', ('station_id', '=', False), ('station_id.responsible_id', '=', user.id)]</field>
            <field name="groups" eval="[(4, ref('max_betong_security.group_betong_tram'))]"/>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="True"/>
            <field name="perm_create" eval="False"/>
            <field name="perm_unlink" eval="False"/>
        </record> -->

    </data>
</odoo>
